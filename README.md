# YouTube AI Search

Веб-приложение и Telegram-бот для поиска бесплатных AI-видео на YouTube.

## Возможности

- 🔍 **Веб-интерфейс** — поиск видео через браузер на `http://localhost:8080`
- 🤖 **Telegram-бот** — команда `/halyava` или `/free` для поиска халявы
- 🎯 **AI-фильтр** — поиск только по ключевым словам AI/LLM
- ✅ **Free-фильтр** — видео со словом "free" в заголовке
- 📅 **Фильтр по дате** — только видео за последние 24 часа
- 🐋 **Docker** — быстрый запуск через контейнеры

## Установка (Docker - Рекомендуется)

```bash
# Клонировать репозиторий
git clone <repo-url>
cd parse

# Собрать и запустить проект
docker-compose up -d --build
```

## Настройка

Создайте файл `.env`:

```env
# Прокси (если нужен для YouTube)
PROXY_URL=http://user:pass@host:port

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

## Запуск (Docker)

Запуск всех сервисов (Веб + Бот):
```bash
docker-compose up -d
```

Просмотр логов:
```bash
docker-compose logs -f
```

### Локальный запуск (без Docker)

1. Установить зависимости: `pip install -r requirements.txt`
2. Запустить веб-приложение: `python app.py`
3. Запустить бота: `python telegram_bot.py`

## Структура проекта

```
parse/
├── app.py              # Flask веб-приложение
├── telegram_bot.py     # Telegram бот
├── templates/
│   └── index.html      # Веб-интерфейс
├── static/
│   ├── css/style.css   # Стили
│   └── last.html       # Последний результат поиска (генерируется)
├── youtube-search-python/  # Локальная библиотека с патчем
├── .env                # Переменные окружения (не в git)
└── .gitignore
```

## AI-ключевые слова

Поиск ведётся по следующим ключевым словам:
- `ai`, `chatgpt`, `gpt`, `openai`, `sora`, `dall-e`
- `claude`, `anthropic`, `gemini`, `deepmind`
- `llama`, `mistral`, `grok`, `deepseek`, `qwen`
- `stable diffusion`, `midjourney`, `runway`, `kling`
- и другие...

## Лицензия

MIT